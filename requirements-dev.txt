# Test / QA tooling. Not needed to RUN the app, only to TEST it.
# Install with:  pip install -r requirements-dev.txt
pytest>=8.0
pytest-cov>=5.0      # coverage measurement (pytest --cov)
httpx>=0.27          # required by FastAPI's TestClient (used from phase T3 onward)
